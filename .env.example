# Environment Variables – Ke-Salary-Calculator
#
# Copy this file to .env and fill in the real values.
# Never commit .env to source control.
#
# These variables must also be added in:
#   Vercel → Project → Settings → Environment Variables
# See docs/VERCEL_SETUP.md for full deployment instructions.

# ── Supabase ────────────────────────────────────────────────────────────────
# Your Supabase project URL (found in Project Settings → API)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase service-role key – server-side only, never expose to the browser
# (found in Project Settings → API → service_role key)
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# ── PayPal ───────────────────────────────────────────────────────────────────
# Set to "sandbox" for testing or "live" for production
PAYPAL_MODE=sandbox
