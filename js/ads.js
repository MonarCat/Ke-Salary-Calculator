/**
 * /js/ads.js
 *
 * RETIRED — the Afams house ad (video banner + rotating sidebar images) has
 * been completely removed per request. This file previously rendered that
 * ad into [data-ad-slot="banner"] / [data-ad-slot="sidebar-media"]
 * elements; those elements have also been removed from the 3 pages that
 * had them (admin-auth.html, premium-thank-you.html, index.html).
 *
 * Left in place as an intentional no-op (rather than removing the
 * <script defer src="/js/ads.js"> tag from every page that references it)
 * so this is a one-line revert if ever needed, matching the same pattern
 * used for assets/js/disclaimer-banner.js and assets/js/ad-network-loader.js.
 */
